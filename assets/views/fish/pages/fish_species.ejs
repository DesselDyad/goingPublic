<!DOCTYPE html>
<html>
<% include ../partials/head %>
    <body>
        <% include ../partials/header %>
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <% include ../partials/locals_errors %>
                    <% include ../partials/form_errors %>
                </div>
                <div class="center col-xs-12">
                    <% if( typeof(singleFishSpecies) != "undefined" && singleFishSpecies.length != 0) { %>
                        <h1><%= singleFishSpecies[0].species.name %></h1>
                        <% singleFishSpecies.forEach(fish => { %>
                            <div class="swipe_div">
                                <article>
                                    <a href=<%="/singleFish/" + fish._id %> alt=<%="/singleFish/" + fish._id %> title=<%="/singleFish/" + fish._id %> >
                                        <img src=<%="/img/fish/fish/" + fish.images[0] %> alt=<%="/img/fish/" + fish.images[0] %> title=<%="/img/fish/" + fish.images[0] %> class="img-responsive" id="latest_img">
                                    </a>
                                    <section>
                                        <p>
                                            <span class="label_span">Description: </span><span id="description_span"><%= fish.description%></span>
                                        </p>
                                        <p>
                                            <span class="label_span">Species: </span>
                                            <a id="type_a" href=<%= "/singleFishSpecies/" + fish.species._id %> alt=<%= "/singleFishSpecies/" + fish.species._id %> title=<%= "/singleFishSpecies/" + fish.species._id %> >
                                                <%= fish.species.name%>
                                            </a>
                                        </p>
                                        <p>
                                            <span class="label_span" width="100">Weight: </span><span id="weight_span">
                                                <%= fish.weight %> kg
                                            </span>
                                        </p>
                                        <p>
                                            <span class="label_span" width="100">Location: </span>
                                            <span>
                                                <a id="location_a" href=<%= "/singleLocation/" + fish.location._id %> title=<%= "/singleLocation/" + fish.location._id %> alt=<%= "/singleLocation/:" + fish.location._id %> >
                                                    <%= fish.location.name %>
                                                </a>
                                            </span>
                                        </p>
                                        <p>
                                            <span class="label_span">Date Caught: </span><span id="date_caught_span">
                                                <a href=<%= "/loadSingleDate/" + fish.date_caught %> title=<%= "/loadSingleDate/" + fish.date_caught %> alt=<%= "/loadSingleDate/" + fish.date_caught %> >
                                                    <%= fish.date_caught %>
                                                </a>
                                            </span>
                                        </p>
                                        <p>
                                            <span class="label_span">Caught By: </span><span id="caught_by_span">
                                                <%= fish.caught_by.local.account.username %>
                                            </span>
                                        </p>
                                    </section>
                                </article>
                            </div>
                        <% }) %>
                    <% } else if( fish_species != "undefined" ) { %>
                        <% if(fish_species.length != 0) { %>
                            <h1>Fish Species that have been successfully caught</h1>
                            <% fish_species.forEach(species => { %>
                                <div class="swipe_div">
                                    <article>
                                        <section>
                                            <p>
                                                <a id="type_a" href=<%= "/singleFishSpecies/" + species._id %> alt=<%= "/singleFishSpecies/" + species._id %> title=<%= "/singleFishSpecies/" + species._id %> >
                                                    <h2>
                                                        <%= species.name%>
                                                    </h2>
                                                    <img src=<%="/img/fish/fish_species/" + species.images[0] %> alt=<%="/img/fish_species/" + species.images[0] %> title=<%="/img/fish_species/" + species.images[0] %> class="img-responsive" id="latest_img">
                                                </a>
                                            </p>
                                            <p>
                                                <span class="label_span">Description: </span><span id="description_span"><%= species.description%></span>
                                            </p>
                                            <p>
                                                <span class="label_span">External Reference: </span><span id="reference_span">
                                                    <a href=<%= species.external_ref%> title=<%= species.external_ref%> alt=<%= species.external_ref%> >
                                                        <%= species.external_ref%></span>
                                                    </a>
                                            </p>
                                        </section>
                                    </article>
                                </div>
                            <% }) %>
                        <% } %>
                    <% } %>
                </div>
            </div>
        </div>
        <!-- /container -->
        <% include ../partials/footer %>
    </body>
</html>